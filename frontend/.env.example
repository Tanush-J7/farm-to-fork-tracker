# Uses the deployed Render API by default — no .env file needed for most setups.
# Uncomment only if you run the backend locally instead.
# VITE_API_URL=http://localhost:5000/api

VITE_CONTRACT_ADDRESS=
VITE_RPC_URL=http://127.0.0.1:8545
